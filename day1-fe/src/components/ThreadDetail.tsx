// import { useState } from 'react'
// import { useParams } from 'react-router-dom'
// import ThreadDummy from '../../utils/fake_data/ThreadDummy.json'

// const ThreadDetail = () => {
//     const { id } = useParams();
//     const [Thread] = useState(ThreadDummy);
//     const element = Thread.find((el) => el.id === Number(id));
//     return element ? <p>{element.content}</p> : null;
// }

// export default ThreadDetail;